import logoHorizontal from '~/assets/images/logo/logo-horizontal.svg'

/** Header and footer wordmark (SVG from `assets/images/logo/`). */
export const LOGO_HORIZONTAL_SRC = logoHorizontal

/** Open Graph default image served from `public/og/`. */
export const OG_IMAGE_PATH = '/og/og-share.jpg'
